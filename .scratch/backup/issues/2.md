# #2 引擎骨架 tracer bullet：门面+内容注册表+Schema 校验+版本化存档+闭关循环

- 状态：CLOSED
- 标签：（无）
- 创建：2026-08-29T15:40:11Z　更新：2026-08-30T04:26:56Z
- 原生阻塞（未关闭的阻塞者）：0

---

## Parent

#1（MVP 规格）

## What to build

打通第一条 tracer bullet：Monorepo 脚手架（core / web / editor 占位 / content+schemas）；引擎门面 createGame（注入内容注册表、存档快照、时钟、随机源，返回命令 API、查询 API、事件流）；内容注册表 + JSON Schema 校验（npm run content:check 作为硬门禁）；版本化存档快照与迁移机制；content/config/ 六境界（不入流→三流→二流→一流→绝顶→宗师）与闭关参数；「闭关」命令与修为增长（离线结算全额、受 config 上限）；Web 壳最小界面（开始/停止闭关、修为与境界显示、本地存档）。

## Acceptance criteria

- [ ] 打开网页可开始/停止闭关，修为随时间增长；离线时长按闭关结算，受 config 上限
- [ ] 刷新浏览器后存档仍在；存档带版本号，迁移机制存在且有测试
- [ ] createGame 注入固定种子 rng 与可拨动假时钟，核心测试全部确定性通过
- [ ] content:check 对非法内容报错并以非零码退出
- [ ] 引擎代码零题材词汇、零写死数量（境界序列等全部来自 config）
- [ ] 测试全部经由引擎门面这一唯一缝合点，TDD 建立

## Blocked by

None (can start immediately)


---

## 评论

**tomatoj23** @ 2026-08-30T03:49:47Z:

已完成（commit `cae6dab`，已推送 main）。

**交付**：Monorepo 脚手架（`packages/core` + `apps/web` + `apps/editor` 占位 + `content/` + `schemas/`）、引擎门面 `createGame(content, save, clock, rng)`、`ContentRegistry` 引用完整性校验、`npm run content:check` 硬门禁、版本化存档快照与迁移机制、`content/config/realms.json` 六境界与闭关参数、Web 壳最小界面（闭关开始/停止、修为与境界、本地存档、离线结算）、editor 占位包。

**验证**：typecheck 全绿；core 17/17 测试通过（含 engine-purity 禁题材词守卫）；content:check 4/4 OK 且非法内容非零拒绝已实测；web 构建成功并预览通过。

**过程中的两处整改**（code-review 双轴审查发现）：`stage` 术语漂移全量改名为 `realm`；Web 壳修复 cleanup 泄漏、toggle 误启动全部活动、存档加载失败无兜底三处缺陷。

**说明**：本票关闭后，`content/config/` 的 `activity` 与 `resource` 等概念沿用，与后续票一致；`realm` 为「境界」的英文名（术语表明确避免「阶段/Level」）。

