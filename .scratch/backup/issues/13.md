# #13 CloudBase 云存档：SaveStore 实装+自动同步

- 状态：OPEN
- 标签：ready-for-agent
- 创建：2026-08-29T15:40:37Z　更新：2026-08-30T03:49:33Z
- 原生阻塞（未关闭的阻塞者）：0

---

## Parent

#1（MVP 规格）

## What to build

CloudBase 云存档：`SaveStore` 接口的 CloudBase 实现（Web SDK）；玩家身份绑定；自动同步策略（本地先行、后台异步上传，离线可玩）；冲突以快照版本号新者胜；服务端只存取 JSON 快照、不解读业务逻辑。**CloudBase SDK 只进 `@idlerpg/web` 的 workspace 依赖，禁止全局安装或改动本机环境（ADR-0007）**。

## Acceptance criteria

- [ ] 同一身份在另一浏览器可拉回最新存档
- [ ] 断网时游戏照常进行，联网后自动补传
- [ ] 存档为版本化快照，读取时走迁移机制
- [ ] 云端不耦合任何业务逻辑，SaveStore 可替换为内存/其他实现
- [ ] 依赖只进 workspace 清单，`npm run content:check` 与全量测试在本地孤立环境中通过

## Blocked by

- #2（引擎骨架 tracer bullet）

## 更新说明（2026-08-30）

补环境约束：ADR-0007 是第三条硬性标准，接入 CloudBase SDK 时最容易"顺手全局装 CLI 或改全局配置"，故在此票显式声明。
