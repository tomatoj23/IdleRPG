# Review Part 6 of 11 - source lines 1325-1579
> Excerpt of docs/archive/grilling-session-2026-08-30.md, for review only. Original file NOT modified.

## 十六、Round 16：1vN 确认 + 「境界」术语质疑

### 16.1 用户确认

- ✅ **秘境战斗支持多目标（1vN）**，1v1 作为特例（用户："1vN，同意你的建议"）
- ✅ 天赋系统：**不建天赋树**，改用**境界突破时的偏向选择**（Round 15.1 方案）
- ✅ primitive 补 **`heal`**、**`dispel`**
- ✅ 伤害计算管线 12 阶段（Round 15.6）
- ✅ 套装效果需 `equippedSet` condition 维度
- ✅ 战斗/武学/装备穷举（Round 15.3–15.7）全部认可
- ⏸ 战力 / 等级 / 功力命名之争**继续搁置**

### 16.2 术语质疑：「境界」是不是仙侠的？

**用户原话**："武侠里有'境界'这种东西吗？这不是仙侠的吗？"

**查证结论：质疑基本成立，但错的是"境界"这个词，不是那套序列名。**

**"境界"在武侠 vs 修真的用法差异**：

| | 用法 | 例子 |
|---|---|---|
| **武侠** | 对**造诣程度**的描述性词汇，**不是等级体系** | 古龙"手中无剑、心中有剑"是一种境界；金庸更多用"造诣""火候""功力" |
| **修真/仙侠** | **等级体系本身的名称** | 炼气→筑基→金丹→元婴→化神；"突破境界"是修真标配短语 |

**问题不在词源，在当代玩家认知**：读过修真小说的人看到"境界突破"第一反应就是仙侠。且与 `style-guide.md:22` 已禁的修仙语汇线（金丹期、元婴、灵气、渡劫、飞升）同源——**"境界"是漏网之鱼**。

**但序列名本身是纯正江湖语汇，必须保住**：
- "一流高手""二流高手""三流货色""不入流"——武侠评价武功高低的常用词
- "绝顶高手"（金庸常用）、"一代宗师"（武侠核心概念）
- 修真绝不会用"三流""不入流"命名境界（它们用"炼气三层"）
- **这套序列是我们最武侠的资产之一**

**结论：换统称，保留序列。**

### 16.3 换词候选与推荐

| 候选 | 武侠用法 | 问题 |
|---|---|---|
| **造诣** ✅ | "武功造诣""颇有造诣"——武侠正宗，指"达到的程度"，正好是进度主轴 | 与 `CONTEXT.md:83` 显示档位的"造诣描述"撞名，需把后者改掉 |
| 火候 | "火候未到""火候已足"——武侠常用 | 偏指技艺熟练度，做进度主轴略窄 |
| 功力 | "功力深厚" | 更像"当前强度"不像"层级"；且 Round 14 推荐它做战力/强度指标 |
| 品流 / 流品 | 与序列前四档（不入流/三流/二流/一流）高度契合 | 后两档"绝顶/宗师"不配套 |
| 保留境界 | 武侠里确有其词 | 玩家认知已被修真占据 |

➡️ **推荐把「境界」改为「造诣」**，同时把 `CONTEXT.md:83` 的显示档位"数值→**造诣**描述"改叫 **「火候」**。

改完的语义分工：

| 术语 | 是什么 | 序列 |
|---|---|---|
| **造诣**（原"境界"） | 你在江湖上的层次，进度主轴 | 不入流 → 三流 → 二流 → 一流 → 绝顶 → 宗师 |
| **火候**（原"造诣描述"） | 你当前实战的强弱，由数值推导 | 不堪一击 → 初窥门径 → 稍有所成 → 登堂入室 → 炉火纯青 → 出神入化 → 返璞归真 |

一个是你**练到了哪一层**，一个是你**现在打得怎么样**——本就该分开命名。

### 16.4 连带问题：「突破」要不要一起换？

`CONTEXT.md:22` 的"突破"（Avoid: 升级）同样有嫌疑——"突破境界"是修真标配短语。武侠里更常说"武功大进""更上一层楼""功力精进"。

➡️ 助手倾向**保留**："突破"在游戏语境里已很中性（"突破瓶颈"各行各业通用），且只换"境界"就足以消除仙侠联想。**待用户决定。**

**若换**，武侠候选："精进""更进一阶""迈入"。

### 16.5 影响面（若决定换词）

需同步修改的位置（落正式文档时一并处理）：
- `CONTEXT.md:9-10`（境界词条与其序列）、`:22`（突破）、`:83`（显示档位/造诣描述）
- 本会话全文中所有"境界"与"显示档位"的提法
- `ADR-0001:3`（境界突破）、`ADR-0005:6`（境界提供基础数值与门槛）、`ADR-0006:7`（事件流的 attackerRealm）
- `content.md` 的 `config/` 注释（realms → 需更名）
- `style-guide.md:14`（境界序列用词表）
- `ui-direction.md:34,36`（"环上 6 境界刻度""推荐境界"）

---

## 十七、Round 17：xkx100 的 50 档查证 + 「修为」质疑 + 引擎不写死

### 17.1 xkx100 的 50 档原文（用户追问"是哪 50 个"）

**出处**：`docs/research/xkx100-kungfu-combat.md:175-177`，摘录自 xkx100 源码 **`cmds/skill/skills.c` L10-21**。
**映射方式**：技能等级（无上限整数，实战常见 0~300+，基本武功经验被引擎限制到 300，`combatd.c` L371/434/608）除以固定步长 → 50 档中文品阶。

> 不堪一击 → 毫不足虑 → 不足挂齿 → 初学乍练 → 勉勉强强 → 初窥门径 → 初出茅庐 → 略知一二 → 普普通通 → 平平淡淡 → 平淡无奇 → 粗通皮毛 → 半生不熟 → 马马虎虎 → 略有小成 → 已有小成 → 鹤立鸡群 → 驾轻就熟 → 青出于蓝 → 融会贯通 → 心领神会 → 炉火纯青 → 了然于胸 → 略有大成 → 已有大成 → 豁然贯通 → 出类拔萃 → 无可匹敌 → 技冠群雄 → 神乎其技 → 出神入化 → 非同凡响 → 傲视群雄 → 登峰造极 → 无与伦比 → 所向披靡 → 一代宗师 → 精深奥妙 → 神功盖世 → 举世无双 → 惊世骇俗 → 撼天动地 → 震古铄今 → 超凡入圣 → 威镇寰宇 → 空前绝后 → 天人合一 → 深藏不露 → 深不可测 → 返璞归真

**要点**：
1. **它也有贬义档**（不堪一击、毫不足虑、不足挂齿、半生不熟、马马虎虎）——xkx100 自己就用了，所以"三流二流"的问题在它那儿同样存在，只是被 50 档的密度稀释了。
2. **后段极度夸张且偏仙侠**（威镇寰宇、空前绝后、天人合一、震古铄今、超凡入圣）——`content-capacity.md:56`"剔除仙侠语汇"剔的就是这些，**该判断正确**。
3. **另有一张独立的知识技能 16 档表**（同文件 L23-28：新学乍用 → 初窥门径 → … → 空前绝后）。

### 17.2 我们的显示档位 7 档 = 5 个取自 xkx100 + 2 个自造

| 我们的 7 档 | 在 xkx100 50 档中的位置 |
|---|---|
| 不堪一击 | 第 1 档 ✅ |
| 初窥门径 | 第 6 档 ✅ |
| **稍有所成** | ❌ **50 档里没有**（它有"略有小成""已有小成"） |
| **登堂入室** | ❌ **50 档里没有**（自造成语） |
| 炉火纯青 | 第 22 档 ✅ |
| 出神入化 | 第 31 档 ✅ |
| 返璞归真 | 第 50 档 ✅ |

**诊断**：自造的两个（稍有所成、登堂入室）是**最平庸的**——既无 xkx100 前段的生动（"半生不熟""马马虎虎"有画面感），也无后段的张力（"一代宗师""返璞归真"有分量）。当前这批词整体处在"正确但无趣"的中段，这也解释了用户为何觉得"新秀好手"不咋地。

**有味道的是两端**：要么生动（粗通皮毛、马马虎虎），要么有分量（一代宗师、返璞归真）。

### 17.3 「修为」是不是修仙词：中等嫌疑，建议保留

- **本义**：佛道用语，指"修行的功夫"。武侠用，仙侠也用，**仙侠用得远多**。
- **嫌疑排序**：`境界`（修真体系核心词，重）> `突破`（修真标配短语，重）> **`修为`（两边都用，中）**
- `style-guide.md:22` 禁用表未列它。

➡️ **建议保留**：它是**经验值，主要出现在 UI 数字与进度条上，很少进叙事文本**；而"境界""突破"是天天出现在玩家眼前的等级名与升级动作，所以那两个更该换。

若仍要换，武侠替代：火候（已建议给显示档位）／积累／进度值。**改动面**：`CONTEXT.md:13`、`:18`、`:100`、`ADR-0001:14`、`content-capacity.md:31`（奖励三通货含"修为"）——比"境界"那组更大。

### 17.4 用户硬性要求：引擎不写死（**比术语重要得多**）

**用户原话**："引擎层面别写死名称和江湖序列数量就行（可以写有几层，但是别不能改，或者改了数量就崩了）"

**翻译成四条技术约束**：

| # | 约束 | 为什么 |
|---|---|---|
| 1 | 江湖序列定义为 `content/config/` 里的**数组**，引擎只遍历 | 增删一档不改代码 |
| 2 | 引擎代码**不出现序列长度字面量**（不能有 `for i in 0..6`） | 否则改数量就崩 |
| 3 | **引擎与存档只存数字等级，武侠词是 UI 层的映射**（⚠️ **Round 18 用户纠正，推翻 Round 17 的 id 化方案**） | 见下方展开 |
| 4 | 数值→档位的映射是**配置表**（`displayTiers`），不是硬编码阈值 | 否则改档数要改代码 |

**第 3 条展开（用户 Round 18 纠正，这是本轮最重要的一条）**：

> 用户原话："引擎里就直接写成等级，我只是要你在 UI 上显示武侠风，这样它只是一个映射关系，明白吗！我在外面删不删档位，跟你等级一毛钱关系没有，只是要修改映射关系。"

**正确的分层**：

| 层 | 存什么 | 例子 |
|---|---|---|
| **引擎 core** | 数字等级 | `realmLevel: 3`、`skillLevel: 147` |
| **存档** | 数字等级 | 同上 |
| **UI / 内容映射表** | 武侠词 | `3 → "高手"`、`147 → "炉火纯青"` |

于是：
- **改名称**（高手 → 好手）：只改映射表，**引擎与存档都不动**
- **删一档 / 加一档**：只改映射表，**引擎与存档都不动**
- **改档位划分**（哪些数值区间对应哪一档）：只改映射表

**⚠️ 助手 Round 17 的错误**：当时建议"存档存 `rankId` 而非索引"，理由是"删档位会让存档错位"。这是**过度设计**——用户指出删档位改的是映射关系，与等级数值无关，所以数字等级天然免疫。本条已按用户方案改写。

**唯一需要迁移的例外**：**改变等级上限**（如境界从 6 级减到 4 级）。此时 `level: 5`、`level: 6` 的存档会越界，需要一条迁移规则把它们收敛到新上限。这不是"改映射"，是"改量程"，属于 `ADR-0002` 版本化迁移的范畴。

**注意区分两条原则**（不要混淆）：
- **内容条目**（武功、装备、怪物、兽）：`content.md:30` 的"**id 一经发布不可变更**"**继续有效**——它们有语义、有交叉引用，必须 id 化
- **等级 / 档位序列**：**用数字 + 映射表**，不需要 id（用户方案）

**同样适用于**（凡"有顺序的数值序列"，都是数字 + UI 映射）：
江湖序列 6 级／显示档位（Round 18 改为 **50 档**）／词缀阶位 T1–T3／伤害占比 4 档／剩余生命 5 档／秘境层数。

**不适用**（仍需 id 化 + 枚举在 config）：七系、武功品阶、脉、门派、primitive 类型、事件/命中类型。

> ⚠️ **Round 19 修正**：本条原把「装备稀有度」也列入"需 id 化"，**已更正**——稀有度的规则（词缀条数 0/1-2/3-4/5-6）在表里，可数值化为 0–3 + 映射表（见 A.17）。**真正的标准不是"有没有语义"，而是"引擎是否按具体值分支"**（见 Round 19.4）。

### 17.5 Round 16–17 术语决策现状

| 术语 | 状态 |
|---|---|
| 境界 → **造诣** | 助手推荐，**待用户定** |
| 不入流/三流/二流/一流/绝顶/宗师 → **新秀/好手/高手/名家/宗师/泰斗** | 助手推荐，用户"先这么着"（可接受但不满意），**待最终定** |
| 突破 → **破关**（呼应闭关） | 助手推荐，**待用户定** |
| 显示档位"造诣描述" → **火候** | 助手推荐，**待用户定** |
| 修为 | **建议保留**（中等嫌疑但改动面大） |
| 山头 → 脉（`lineageId`） | ✅ 已定 |
| 战力/功力/等级 | ⏸ 搁置 |

**用户明确的核心立场**：具体名称可以慢慢磨，**但引擎绝不能写死**——名称与数量必须是内容，改了不能崩。

---

## 十八、Round 18：50 档照搬 + 映射方案纠正 + 术语定案

### 18.1 用户四项决定

| # | 事项 | 决定 |
|---|---|---|
| 一 | 显示档位 | **原样照搬 xkx100 的 50 档**（取代原有 7 档） |
| 二 | 「修为」 | **保留** |
| 三 | 等级与武侠词的关系 | **引擎就用数字等级，武侠词只是 UI 的显示映射**（纠正助手 Round 17 的 id 化方案） |
| 四 | 「突破」 | **保留**，不改成"破关" |

### 18.2 显示档位改为 50 档（含风险提示）

**决定**：照搬 xkx100 的 50 档，完整列表见 **Round 17.1**（出处 `cmds/skill/skills.c` L10-21）。

**⚠️ 必须让你知道的风险**（助手职责，决定权在你）：50 档的**后段有仙侠/修真嫌疑**，与 `style-guide.md:22` 已禁的修仙词同源：

| 档位 | 嫌疑 |
|---|---|
| **超凡入圣** | 最重——"入圣"是宗教/修真语汇 |
| **天人合一** | 重——道家哲学术语，修真小说常用 |
| 震古铄今 / 威镇寰宇 / 空前绝后 | 中——极度夸张，武侠里少见这种量级 |
| 神功盖世 / 举世无双 / 惊世骇俗 / 撼天动地 | 轻——夸张但武侠可用 |

`content-capacity.md:56` 当初写"剔除仙侠语汇"针对的就是这些。**若照搬 50 档，建议至少替换「超凡入圣」与「天人合一」两档**（可用同位置的武侠词替代，如"出神入化"附近的表达），其余保留。

**映射方式**：xkx100 是"技能等级 ÷ 固定步长 → 50 档"。我们照搬时有两种取法：
- **A. 数值区间映射**（`displayTiers` 表：数值区间 → 档位词）——与 `content-capacity.md:32` 原设计一致
- **B. 等级 ÷ 步长**（照搬 xkx 做法）

➡️ 推荐 **A**，因为我们的数值是连续增长的战斗数值，不是 0-300 的技能等级；且区间表可随时调整疏密。

**附带好处**：50 档比 7 档**密集得多**，正好补上 A.13 诊断的"中频反馈缺失"——玩家每提升一点战力，档位词就可能跳一格，这是个高频的成长提示。

### 18.3 映射方案（用户纠正，本节取代 Round 17.4 第 3 条）

> 用户原话："**引擎里就直接写成等级，我只是要你在 UI 上显示武侠风，这样它只是一个映射关系，明白吗！我在外面删不删档位，跟你等级一毛钱关系没有，只是要修改映射关系。**"

**助手承认错误**：Round 17 建议"存档存 `rankId` 而非索引"是**过度设计**——把"映射"误当成了"数据"。用户方案更简单也更正确，且更符合 `ADR-0004`（引擎里连"高手"这个词都不存在）。

```
引擎 core  ──→  数字等级 (level: 3)
存档        ──→  数字等级 (level: 3)
UI 映射表   ──→  "高手"        ← 改词、增删档位只动这里
```

**区分两类东西**（易混淆，务必记住）：
- **纯数值序列**（江湖等级、显示档位、词缀阶位、伤害占比、剩余生命档、秘境层数）→ **数字 + 映射表**
- **有语义的枚举**（七系、武功品阶、脉、门派、primitive 类型、事件/命中类型）→ **必须有稳定 id**，被内容交叉引用，`content.md:30` 的"id 一经发布不可变更"对其继续有效
  - ⚠️ **Round 19 修正**：**稀有度不在此列**（它可数值化，见 A.17）。且**"引擎按 id 分支"这个说法也被 Round 19.4 修正**——正确要求是"引擎**不得**按具体值分支，分支条件须数据化"

**唯一需要迁移的例外**：改**等级上限**（如境界 6 级 → 4 级），超出的存档要收敛，属 `ADR-0002` 版本化迁移。改映射、改名称、增删档位都**不需要**。

### 18.4 影响面（落正式文档时处理）

| 位置 | 改动 |
|---|---|
| `CONTEXT.md:83` | 显示档位从 7 档改为 50 档（完整列表） |
| `content.md:51` | 显示档位命名语汇同步为 50 档 |
| `content-capacity.md:56` | 显示档位表格更新；"剔除仙侠语汇"一句需重新裁定（照搬 50 档后该句如何处理） |
| `content/config/displayTiers` | 从 7 条扩为 50 条区间映射 |
| `ADR-0006:7` | 事件流 `attackerDisplayTier`/`targetDisplayTier` 由七档改为 50 档（离散枚举值变化） |
| 本会话全文 | 所有"显示档位 7 档"的提法统一改为 50 档 |

### 18.5 术语最终状态

| 术语 | 状态 |
|---|---|
| 境界 | **保留**（用户未要求改动；且按 18.3，它只是 UI 映射的一层，引擎里是数字等级） |
| 修为 | ✅ **保留** |
| 突破 | ✅ **保留**（明确否决"破关"） |
| 山头 → 脉（`lineageId`） | ✅ 已定 |
| 战力 / 功力 / 等级 | ⏸ 搁置 |
| 江湖序列用词（新秀/好手/高手/名家/宗师/泰斗） | 用户"先这么着"，**名称是映射表内容，随时可改** |
| 显示档位 | ✅ **改用 xkx100 的 50 档** |

---

