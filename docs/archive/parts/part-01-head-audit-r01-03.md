# Review Part 1 of 11 - source lines 1-262
> Excerpt of docs/archive/grilling-session-2026-08-30.md, for review only. Original file NOT modified.

# 元素与战斗系统 · 设计会话全记录（临时稿）

> **状态**：临时存档，**非正式文档**，不得作为决策依据引用。
> **用途**：保存 grilling 会话的完整推理链，防止上下文超限导致细节丢失。
> **处置**：全部讨论结束后，据此生成正式的 `CONTEXT.md` 词条与 `docs/adr/` 决策记录，届时删除本文件并清理 `.gitignore` 中的对应行。
> **日期**：2026-08-30
> **参与者**：用户（决策）／助手（事实查找、发散、推荐）

> **⚠️ 全局说明（读旧章节时请注意）**
> 1. **显示档位**：Round 12–17 期间按"7 档"讨论，**Round 18 用户决定改为照搬 xkx100 的 50 档**。旧章节中"显示档位 7 档"的表述已被该决定取代，完整列表见 **Round 17.1**，新决定见 **Round 18.2**。
> 2. **等级与武侠词的关系**：Round 17.4 曾建议"存档存 id 不存索引"，**Round 18 被用户纠正并推翻**。正确方案是"**引擎与存档存数字等级，武侠词只是 UI 映射表**"，见 **Round 18.3**。以 18.3 为准。
> 3. **术语**：「境界」「修为」「突破」**均保留**（Round 18 定）；「破关」方案被否决。
> 4. **稀有度的因果**（Round 20 用户纠正，推翻 Round 19 的表述）：**先定稀有度 → 再按稀有度生成词条**。稀有度是因、词条数是果，不是由词条数反推。装备生成流程见 20.1。
> 5. **映射表的每一项是配置对象**，不只是一个名称——下游规则/解锁清单/呈现参数都挂在同一处（Round 20 推广，见 A.17）。

---

## 目录

- 零、前置：项目审计结论（本会话的起因）
- 一、Round 1：C3 伤害维度 / C4 槽位语法
- 二、Round 2：七系与组合系统（用户提出新方向）
- 三、Round 3：风雷加入、七系表定稿
- 四、Round 4：护甲、抗性、兵刃伤害
- 五、Round 5：双向博弈（玩家也要有抗性）
- 六、Round 6：内功/外功二分 + POE 抗性的讨论
- 七、Round 7：出招顺序 + 内力系统
- 八、Round 8：闪避
- 九、Round 9：代价设计 + 主动释放扩展性 + 槽位修正
- 十、Round 10：文本片段化 + 自伤机制 + 引擎层三个坑
- 十一、Round 11：宠物/召唤物的调研与立场
- 十二、Round 12：闪避文本、兽文本、Q34 重述
- 十三、Round 13：兽的引擎预留 + 门派山头 + 变强反馈
- 十四、Round 14：专精式强化、术语换词、门派机制全景
- **十五、Round 15：天赋系统评估 + 战斗/武学/装备维度穷举（含伤害管线）**
- 十六、Round 16：1vN 确认 + 「境界/突破」术语质疑
- 十七、Round 17：xkx100 的 50 档查证 + 「修为」质疑 + 引擎不写死
- **十八、Round 18：50 档照搬 + 「数字+映射」方案纠正 + 术语定案**（⚠️ 含对 Round 17 的推翻）
- **十九、Round 19：xkx100 档位机制调研 + 数值化穷举 + 枚举增删的前提**（⚠️ 含对 Round 18 归类的修正）
- **二十、Round 20：稀有度因果修正 + 映射表结构扩展 + 显示档位映射定案**（⚠️ 含对 Round 19 稀有度表述的修正）
- **二十一、Round 21：#3 秘境结构（一）——依据、三个矛盾、十维穷举**（⚠️ 补记：当时未落文档）
- **二十二、Round 22：#3 秘境结构（二）——定时波次深化 + 手动目标选择 + intent 三维度**
- **二十三、Round 23：#1 装备底材与词缀池——掉落链、物品等级、标签化、离线掉落玩家可选**
- **二十四、Round 24：底材分层的修正（推翻 23.3）+ POE 长期成长调研（数值线与构筑线并行）**
- 附录 A：已达成结论清单（A.1–A.20）
- 附录 B：未决问题与下一步
- 附录 C：文档完整性检查记录

---

## 零、前置：项目审计结论（本会话的起因）

本次会话开始于一次项目结构与文档审计。审计发现的问题中，有两条属于"设计决策未定"，必须用户拍板，于是进入 grilling。其余为纯执行项，待设计定稿后一并修复。

### 0.1 清理状态（已完成）

| 项 | 结果 |
|---|---|
| `.scratch/` | 用户已删除；git 中记为 56 个 `deleted`，**尚未提交** |
| `node_modules/` | 已删除（3778 文件 / 71.5 MB，含三个断裂的 `@idlerpg/*` symlink） |
| `package.json` / `pnpm-lock.yaml` / `pnpm-workspace.yaml` / `tsconfig.json` / `.npmrc` | 全仓扫描零命中 |
| `content/` | 只有 `style-guide.md`，无 JSON、无 `schemas/`、无 `assets/` |
| 远端 | `tomatoj23/IdleRPG` 已清空：1 提交、**0 issue**；本地 12 提交未推送 |
| `.scratch/backup/` 中的 17 个 issue 快照 | 仍可从提交 `6d4e169` 恢复；**用户明确表示不恢复，重新做** |

### 0.2 审计发现的【冲突】8 条

- **C1 稀有度命名的决定出处错指**：`ADR-0005:5` 写"见 ADR-0008"，但 ADR-0008 是《内容承载边界》，只在末句顺带说"命名见 CONTEXT.md"，并未记载该决定。真正记载它的是 `design/content-capacity.md:48-49`，而该文件自称"已降级为分析记录，不再作为唯一决策来源"。
- **C2 `ui-gen-prompts.md` 未同步命名语汇**：`content-capacity.md:7` 声称已同步，实际 `ui-gen-prompts.md:76` 仍写"槽边框有**品质**区分（三档）"、`:159` 写"品质分三档"。① "品质"是 `CONTEXT.md:65` 列入 Avoid 的旧词；② "三档"与四档稀有度（寻常/精良/罕见/绝世）矛盾。已用 grep 坐实：该文件搜"寻常/精良/罕见/绝世/稀有度"**零命中**。
- **C3 伤害类型 vs 元素标志**：`ADR-0006:7` 说事件流带"**元素标志**"；`ADR-0008:3` + `content.md:41` 用 `damageType` **伤害类型**（割/刺/瘀/内伤/抓伤）。两者语义不同，`CONTEXT.md` 均未收录。**→ 本会话主要议题**
- **C4 战斗文本槽位有两套命名**：`style-guide.md:30` 用 `{攻击者}{受击者}{招式}{动词}{部位}{后果}{伤害}`；`ui-direction.md:47` 与 `content-capacity.md:35` 引用 xkx100 的 `$N/$n/$w/$l/$d`。关键事实：`research/xkx100-kungfu-combat.md:264` **自己建议不要照搬** `$` 记号，改用 `{attacker}` 风格——两份引用它的文档把"被研究对象"误当成了建议。**→ 本会话主要议题**
- **C5 `style-guide.md` 自指矛盾**：`:21` 把"玩家"列为禁止现代词，但 `:8` 自己写"第三人称或对**玩家**的第二人称'你'"，`CONTEXT.md` 全篇用"玩家"定义术语。禁止词表的作用域未声明。
- **C6 research 建议已被 supersede 但未标注**：`xkx100-kungfu-combat.md:262-265` 建议"武学分类枚举（**内功**/轻功/拳掌/兵刃/身法招架）"、给 combat-text 用 `minLevel`——与 `CONTEXT.md:47` Avoid「内功」、`:11` Avoid「等级」冲突。
- **C7 `content-capacity.md:72` 说"config 每集合一文件是惯例，`content.md` 已说明"**——未说明。`content.md` 只有 `config/` 目录注释与 id 规则里"config 集合豁免序号段"。
- **C8 `sect.masters` 字段缺失**：`CONTEXT.md:117`、`ADR-0008:8`、`content-capacity.md:29,41` 三处说"师父归入门派条目字段"，但 `content.md:38-45` 的字段约定里没有 `masters`。

### 0.3 审计发现的【术语缺口】

`CONTEXT.md` 全文 grep `奇遇|世界文本|lore|区域|怪物|药材|生产活动|采集|师承|装备槽|战力|物品表|伤害类型|元素标志|炼丹`，只有 4 处命中（`区域` 在第 91 行藏在「归属」词条里；`炼丹` 在第 94 行仅作小节标题，第 100/116 行顺带提及）。

**零出现的 13 个术语**：奇遇、世界文本/lore、怪物、药材、生产活动、采集、师承、装备槽、战力、物品表、伤害类型、元素标志、内力（后续新增）。

### 0.4 审计发现的【结构】4 条

- **S1** `docs/agents/domain.md:17-38` 通篇用 `src/` 举例，与 ADR-0002 的 monorepo（`core` 包 + `apps/*`）不符——模板残留。
- **S2** `content.md:22-25`：`schemas/` 和 `assets/` 画在 `content/` 树块里却无缩进（实为顶层），极易误读。
- **S3** `docs/design/` 与 `docs/research/` 无入口指针：`AGENTS.md` 只指向 `adr/0007`、`agents/*`、`content/style-guide.md`。`design/ui-direction.md` 完全无人引用（孤儿）。
- **S4** 无文档定义 monorepo 的包路径规范：`core` 从未写出 `packages/core`；`apps/web`（ADR-0002）与 `apps/editor`（ADR-0003）各出现一次，无统一清单。

### 0.5 审计发现的【死链】

| 引用 | 出现处 | 现状 |
|---|---|---|
| `#15` | `content-capacity.md:34,62` | 远端 0 issue，死链 |
| `npm run content:check` | ADR-0003、`content.md`、`style-guide.md`（共 4 处） | 无 `package.json`，脚本不存在 |
| `apps/web/src/App.tsx` | `content-capacity.md:73` | 已随代码清零消失 |
| GitHub issues | `docs/agents/issue-tracker.md` 全篇 | 远端已清空 |

**错字**：`content-capacity.md:29`"商人与任务发布人 **MVG** 不做" → MVP。
**排版**：`ui-direction.md:13-20` 表格断裂（表头 + 分隔行后插入散文，再起一表无表头）。

---

## 一、Round 1：C3 伤害维度 / C4 槽位语法

### 1.1 提出的问题

- **Q1**：MVP 要不要引入"系别"作为参与运算的第二维度？（推荐 A：不要）
- **Q2**：战斗文本槽位用什么语法？（推荐 `{attacker}` 英文花括号）
- **Q3**：槽位全集取哪几个？（推荐 7 个：attacker/defender/move/weapon/limb/damage/consequence）

### 1.2 关键事实

- `research:211` 证实：xkx 的 `damage_type` 只服务"按 damage_type × 伤害数值双维度**选模板**"，**不参与数值运算**。
- `research:264` 原文建议："IdleRPG 应固化一套更小的槽位协议（如 `{attacker}` `{defender}` `{move}` `{limb}`），combat-text 只存模板，聚合引擎做替换"。
- xkx 槽位体系：`$N/$n` 攻击者/防御者、`$P/$p` 你/他代词、`$w/$W` 兵器、`$l` 被击肢体（人类 21 个部位）。

### 1.3 用户回答

> 全按建议，除了 Q1，因为我没看明白 Q1。**我需要不同类型的伤害**，但命名文本可以中式，如"木火土金水"。

用户提出三点设想：
1. 门派武功（无属性或元素性质）+ 带属性装备 → 产生属性伤害效果（打狗棒法+木系装备 → 中毒）
2. 门派（有元素属性，如丐帮火系）+ 不特定门派但影响武功效果的装备 → 持续伤害，装备提高或引发联动
3. 门派武功有属性 + 装备有属性 → 组合（火系武功 爆裂/燃烧/扩散 + 水系武器 流动/治愈/冰寒 = ？）**用户没想好，要求发散**

**用户约束**："前面提到的所有只是举例，不代表最后就会采用，**不要把举例写进文档**，还要进一步讨论，生成的我比较满意的初步方案后再落文档。"

### 1.4 本轮结论

- Q1 答案反转为 **B 方向**：伤害带系别维度，且参与运算。
- Q2、Q3 用户未明确反对，后续被 Round 3 的主辅模型吸收。

---

## 二、Round 2：七系与组合系统

### 2.1 我给出的发散（五行 + 相生相克变异表）

**核心设计选择：相克不要做成"削弱"，要做成"变异"。**
理由：传统"水克火 → 火系伤害 -30%"会让玩家觉得构筑踩雷，是纯负反馈，与 `ADR-0001`"流派由玩家玩出来、不预设标签"相悖——踩雷的构筑没人玩，等于预设了唯一解。

**五行性格**：金=锋锐穿透（破甲、流血）／木=生长蔓延（中毒、缠绕）／水=流动寒（冰寒迟缓、治愈）／火=爆裂燃烧（灼烧、溅射）／土=厚重承载（护体、稳固）。

**相生 5 组**（增强与转化）：木生火=灼烧蔓延／火生土=烬留护体／土生金=攻防一体／金生水=冰寒破甲／水生木=中毒传染。
**相克 5 组**（变异而非削弱）：水克火=蒸汽爆沸（单体转范围+迷雾降命中）／火克金=熔毁（防御归零）／金克木=砍伐（中毒转流血）／木克土=禁锢／土克水=淤泥（减速变定身）。

**属性挂点模型**：
```
招式属性 = 武功自身声明的属性 ?? 继承门派属性 ?? 无
装备属性 = 武器/装备自带的属性，可无
最终效果 = 查组合表
```

### 2.2 用户回答

> 发散的不错，尤其**变异**这里很喜欢，但是：
> **Q4**：是否可加入**风、雷**？这样进一步弱化相克、强化变异、增加组合，需要风雷的机制性格，要与前五系明显区分，**必要时可削减/调整前五系**。
> **Q5**：按推荐。丐帮=火只是举例，先作为缺省。
> **Q6**：参考 Q4，需重新商讨。
> **Q7**：同意建议，但要求：① **不要把内容做进引擎，绝对分离**；② 引擎中的文本描述转为非内容描述（持续伤害、护甲降低、无法攻击，而非"灼烧、中毒、破甲"）；③ **对后台暴露所有可编辑项**（灼烧次数、可否驱除等）；④ 保证未来可添加/移除伤害模式甚至元素属性的扩展空间，**不要过早写死**；⑤ 能否**低耦合模块化**。

### 2.3 我给出的架构方案（回应 Q7）

**两层拆分**：
```
引擎层（core，零题材词汇）
  └─ mechanism primitive 注册表（小、稳定、与题材无关）
内容层（content/）
  ├─ config/elements.json              七系
  ├─ config/element-combinations.json  稀疏组合表
  ├─ config/effect-params.json         primitive 参数 schema
  └─ effects/*.json                    效果 = primitive 组合 + 参数
```

**效果声明式示例**（灼烧）：
```json
{
  "id": "eff-burn",
  "displayName": "灼烧",
  "textKey": "burn",
  "components": [
    { "primitive": "damage-over-time",
      "params": { "ticks": 3, "perTick": 12, "element": "fire", "tickTiming": "turn-start" } },
    { "primitive": "visual-flag", "params": { "flag": "burning" } }
  ],
  "stacking": { "mode": "refresh", "maxStacks": 1 },
  "dispel": { "dispellable": true, "resistance": 0 },
  "expiry": { "mode": "turns", "value": 3, "onDeath": "clear" }
}
```
引擎只读 `components[].primitive` 与 `params`，**永不读 `displayName`**。

**可扩展性的代价表**：

| 未来想做的事 | 需要改什么 | 动引擎吗 |
|---|---|---|
| 加第 8 系 | `elements.json` 加一项 | 否 |
| 删掉一系 | 删一项 + 迁存档 | 否 |
| 加一个新组合 | 组合表加一格 | 否 |
| 加一个新效果（用现有 primitive） | 加 effect JSON | 否 |
| 加一种全新伤害模式 | 注册新 primitive | **是**（只加模块，不改已有代码） |

**代价警告**：① 调试成本（需编辑器与校验器兜底）；② primitive 抽象层若定不稳后期返工（建议参数用通用结构）；③ 组合表结算顺序必须早定（纳入 ADR-0005 的三层管线）。

---

## 三、Round 3：风雷加入、七系表定稿

### 3.1 关键洞察

五行之所以绕不开"相克"，是因为 5 个元素构成闭环（\(Z_5\)），每个元素必须克一个、被一个克。**加到 7 个，闭环就断了**，没有现成的相生相克表，组合规则必须显式声明——这正好把"相克削弱"赶出去，只留"变异"。

### 3.2 对前五系的削减与调整

- **改动 1：金的「流血」→「裂伤」**。流血/中毒/灼烧三者都是持续伤害，三重重叠。改为"裂伤"（目标受到的所有伤害提高）后，金独占**破防/增伤**轴。
- **改动 2：木的「缠绕」→「传染」**。木（缠绕降频）、水（冰寒降频）、风（疾提频）三系抢同一根"频率"轴。木去掉缠绕，专注**疫毒**轴。频率轴留给水和风，一降一升形成对称。
- **改动 3：土的「稳固」、水的「治愈」、风的「连击/闪」、雷的「震/瞬」留 v2**。

### 3.3 七系表（MVP 形态）

| 系 | 轴 | 机制 | MVP 形态 |
|---|---|---|---|
| 金 | 破防 | 裂伤（受伤加深）、破甲 | 纯数值 modifier |
| 木 | 疫毒 | 中毒（持续伤害）、传染 | **真状态** |
| 水 | 滞缓 | 冰寒（降敌方出手频率）、治愈(v2) | 纯数值 modifier |
| 火 | 燃爆 | 灼烧（持续伤害）、爆裂（溅射） | **真状态** |
| 土 | 守护 | 护体（减伤）、稳固(v2) | 纯数值 modifier |
| 风 | 迅疾 | 疾（提己方出手频率）、连击(v2)、闪(v2) | 纯数值 modifier |
| 雷 | 霆爆 | 暴（暴击率/暴伤）、震(麻痹,v2)、瞬(追加打击,v2) | 纯数值 modifier |

**两系新血的差异化**：风是**己方增益**（提速），与水（敌方降频）对称；雷是**瞬时爆发**（暴击），与火（持续燃烧+范围）对称——火是"烧一阵、烧一片"，雷是"一下很疼"。七根轴：破防／疫毒／滞缓／燃爆／守护／迅疾／霆爆。

**关键**：这张表恰好还是 **2 个真状态 + 5 个纯数值**，与用户已同意的 Q7 折中方案严丝合缝，加两系未增加状态系统负担。

### 3.4 组合模型：主属性决定形态，辅属性决定修饰

用户洞察："水武功+火装备"与"火武功+水装备"产生的效果应该不同——这把组合表从 49 格**对称**矩阵变成**有序**矩阵（同系那 7 格除外）。

> **主属性（来自武功/门派）决定这一招"是什么"**——提供该系的核心效果。
> **辅属性（来自装备）决定这一招"额外怎么样"**——对核心效果施加一个修饰。

| 系 | 主位（核心效果） | 辅位（施加的修饰） |
|---|---|---|
| 金 | 破防（降护甲） | 锐化：加深、转为即时 |
| 木 | 中毒（持续伤害） | 蔓延：扩散、叠加加速 |
| 水 | 冰寒（降频） | 流转：范围化、传导 |
| 火 | 灼烧（持续伤害） | 爆裂：溅射、转为爆发 |
| 土 | 护体（减伤） | 沉滞：延时、固化 |
| 风 | 疾（提频） | 迅发：提速、连击化 |
| 雷 | 暴（暴击） | 震荡：触发式、追加打击 |

**组合示例**（有序）：火主+水辅 = 蒸汽爆沸（灼烧转范围+迷雾降命中）／水主+火辅 = 冰火激变（迟缓持续，结束时冰层骤裂爆发）／木主+风辅 = 毒散（叠层快且传染）／风主+木辅 = 毒风（提速且每次附带少量中毒）／雷主+金辅 = 霆裂（暴击时无视部分护甲）／金主+雷辅 = 锐霆（破甲状态下受击触发追加打击）。

**这解决了"全部相生会不会导致最优解唯一"**：辅属性不是"谁都行，越多越好"，而是七种修饰**方向不同**，取舍来自方向而非强弱。

### 3.5 用户回答

> Q4/Q5/Q6/Q7 详见 Round 2 回答；**同意 primitive 参数用通用结构**（`amount`/`duration`/`targetSelector`/`condition`），但因为七系表还没定，伤害/增益类型可能变化，primitive 最终集合等七系表定完再锁。

---

