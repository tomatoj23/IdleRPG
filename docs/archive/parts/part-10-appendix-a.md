# Review Part 10 of 11 - source lines 2215-2580
> Excerpt of docs/archive/grilling-session-2026-08-30.md, for review only. Original file NOT modified.

## 附录 A：已达成结论清单（可直接落文档）

### A.1 战斗系统的完整形态

| 层 | 进攻侧 | 防御侧 |
|---|---|---|
| **战斗形态**（Round 16 定） | **支持多目标（1vN）**，1v1 为特例 | `targetSelector` 需支持 单体／全体／相邻／随机 N 个／除主目标外 |
| **作用方式** | 内功 / 外功（招式字段；怪物攻击也标） | **外功防御（护甲）** / **内功防御** — 2 个属性 |
| **系别性质** | 七系（附加伤害 = 兵刃层 × 系别系数） | 怪物亲和度（config） + 效果抵抗（条件修饰符） |
| **伤害构成** | 兵刃层（恒有）+ 系别层（可有可无） | — |
| **减伤公式** | — | 百分比 + 递减 `减伤 = 防御 / (防御 + K)`，K 在 config |
| **组合** | 主（武功/门派）× 辅（装备）→ 有序查表 | — |
| **资源** | 招式消耗内力 | 内力上限（境界+心法+装备） |
| **命中判定** | 闪避判定 → 命中（**不做招架/格挡**，留 v2） | 闪避率：通用低基调，基础 5-10%，上限约 40%，递减曲线在 config |
| **出招意图** | **intent 源可插拔**（MVP 仅 `auto-source` = 加权轮盘；未来注册 `manual-source` 即支持主动释放，core 零改动） | — |

### A.2 七系表（定稿）

| 系 | 轴 | 主位效果 | 辅位修饰 | 内力性格 | MVP 形态 |
|---|---|---|---|---|---|
| 金 | 破防 | 裂伤（受伤加深）、破甲 | 锐化：加深、转即时 | 耗内破防 | 纯数值 |
| 木 | 疫毒 | 中毒、传染 | 蔓延：扩散、叠加速 | 寄赖/吸内（**需代价**） | **真状态** |
| 水 | 滞缓 | 冰寒（降频）、治愈(v2) | 流转：范围化、传导 | 滋养：回复/上限 | 纯数值 |
| 火 | 燃爆 | 灼烧、爆裂（溅射） | 爆裂：溅射、转爆发 | 倾泻：高耗高爆发 | **真状态** |
| 土 | 守护 | 护体（减伤）、稳固(v2) | 沉滞：延时、固化 | 承载：上限/代偿减伤 | 纯数值 |
| 风 | 迅疾 | 疾（提频）、连击(v2)、闪(v2) | 迅发：提速、连击化 | 轻灵：消耗降低 | 纯数值 |
| 雷 | 霆爆 | 暴（暴击）、震(v2)、瞬(v2) | 震荡：触发式、追加 | 激荡/背水（**需代价**） | 纯数值 |

### A.3 架构原则（用户硬性要求）

1. **引擎零题材词汇**：引擎只认 primitive（`damage-over-time` / `stat-modifier` / `rate-modifier` / `crit-modifier` / `dodge-modifier` / `armor-modifier` / `action-lock` / `visual-flag`），永不读 `displayName`。
2. **效果 = primitive 组合 + 参数**，声明式，走 ADR-0005 三层管线（固定值 → 加法百分比 → 乘法区）。
3. **primitive 参数用通用结构**：`amount` / `duration` / `targetSelector` / `condition`。
4. **所有可编辑项暴露**：ticks、dispellable、resistance、expiry、stacking、onDeath 全部是数据，参数 schema 在 config，编辑器据此自动生成表单。
5. **加系/删系/加组合/加效果（用现有 primitive）都不动引擎**；加新伤害模式才注册新 primitive。
6. **不为未做的系统预留 schema**（ADR-0004:7 扩展留白精神）。

### A.4 primitive 初始集合（8 个，MVP 用 5 个）

`damage-over-time` / **`instant-damage`** / `stat-modifier` / `rate-modifier` / `crit-modifier` / `visual-flag` / `dodge-modifier`（**MVP 使用**）
**`heal`** / **`dispel`**（**Round 15 补**：治疗回复与驱散状态。与 `instant-damage` 同族——都是"对某目标施加一次效果"，加进来不破坏结构）
`armor-modifier` / `action-lock`（**MVP 只注册不使用**，作为抽象层压力测试）

> 注 1：`dodge-modifier` 因 Q26（闪避进 MVP）从"只注册不使用"**提升为 MVP 使用**。它同时承担两个方向：提高己方闪避、降低目标闪避（负向值，土/木/水压制手段）。
>
> 注 2：**`instant-damage` 是 Round 10 补的关键 primitive**。此前"攻击造成伤害"被当作战斗默认行为未建模，导致"立即伤害"在 primitive 层缺失。补上后四个用法共用一套代码：普通攻击（target=敌方）／自伤（target=自身）／反伤（target=攻击者）／溅射（target=相邻）。
>
> 注 3：primitive 参数统一用 `amount` / `duration` / `targetSelector` / `condition`（Round 3 用户确认）。

### A.5 出招与战斗文本

- 出招 = 加权轮盘（intent 源可插拔，见 A.8）；MVP 4 个调制器：冷却 / 内力 / 重复衰减 / 系别亲和
- MVP 一个招式 = 一个条目，**不做多形态**
- 一个招式配**多条文本变体**，随机抽
- 招式文本 = 基础模板 + 系别意象词库（可选槽位）
- **槽位语法 = `{attacker}` 英文花括号（非 xkx 的 `$N`）**
- **槽位全集 9 个**：`{attacker}` `{defender}` `{move}` `{weapon}` `{defenderWeapon}` `{limb}` `{damage}` `{consequence}` `{elementFlavor}`
  - 已删：`{动词}`（应为模板固定文本，槽位化致句式雷同，违 `style-guide.md:46`）、xkx 代词 `$P/$p`（视角是集合级常量）
  - `{elementFlavor}` 无属性时填**中性词，不可为空串**（否则产生"，，"）
- **模板 = 片段序列，非"一句固定话挖洞"**（Round 10 重构）：
  `{opening}{attacker}{moveIntro}「{move}」，{weaponAction}{elementFlavor}，{weapon}{verb}{defender}的{limb}，{consequence}。`
  新增片段：`{opening}` 起势、`{moveIntro}` 招式引出、`{weaponAction}` 兵器动作、`{verb}` 动词
- **片段序列可变长度**：模板自己声明用哪些片段（`segments` 数组），下乘 3 段、绝学 7 段——兑现 `style-guide.md:32`"绝学招式模板可到 3 句的华丽长句"
- **三层门控**：① 致命度门控（致命部位需 伤害档∈{重,濒死} AND 剩余生命档∈{少,危}）；② 搭配约束（兵器类型→动词池→部位的 `limbs` 白名单）；③ 节奏一致（先定 tempo 档：急/缓/稳，各片段只从同档抽）
- **引擎只需"带约束的词库抽取器"**：给定过滤条件，从池中随机取一条。所有门控/搭配/节奏都是数据。

### A.6 内力（MVP 最小形态）

上限（境界+心法+装备）／招式消耗／战斗中持续回复／零消耗基础攻击（内力见底自动切换）／离线用稳态 DPS 公式 O(1) 结算。
**七系 × 内力差异化留 v2**，但代价设计已定（见 B.1 待确认项）：

| 系 | 内力维度的性格 | 代价 |
|---|---|---|
| 金 | 耗内破防 | — |
| 木 | 寄赖 / **吸内** | 不能吸干（下限 25%）／转化效率 40%／自身伤害 ×0.7／可被「固本」抵抗 |
| 水 | 滋养：回复 / 上限 | — |
| 火 | 倾泻：高耗高爆发 | — |
| 土 | 承载：上限 / 代偿减伤 | — |
| 风 | 轻灵：消耗降低 | — |
| 雷 | 激荡 / **背水**（内力越低伤害越高） | 触发阈值 <30%，加成上限 +50%；门户大开（防御同步下降）、闪避降低、需主动维持低内力 |

### A.7 闪避（Q26/Q27/Q28）

- **只做闪避，不做招架/格挡**（留 v2）。命中判定两级：闪避 → 命中。
- **通用低基调**：基础 5-10%，上限约 40%，递减 `实际闪避 = 闪避 / (闪避 + K)`，K 在 config。
- **闪避只让这一次攻击落空，不跳过回合**（否则高闪避拖成无限战斗）。
- **闪避是纯数值，不是状态** → 不破坏"2 真状态 + 5 纯数值"的平衡。
- **离线结算**：期望值，有效输出 = 原始输出 × (1 − 闪避率)。
- **与风系不绑定**：闪避是通用数值，风系只通过**内容层词缀权重**更容易出闪避词缀，引擎不知情。
- **降低闪避**：`dodge-modifier` 对目标施加负值。土（压）／木（缠，走传染线）／水（滞）三种语义均可。

### A.8 主动释放武功的扩展性（用户硬性要求）

- **core 只认 intent 接口，从不问来源**：`intent = { 谁、用什么招、对谁 }`
- `auto-source`（加权轮盘）是 MVP 唯一的 intent 源；未来注册 `manual-source` 即支持玩家主动释放，**core 零改动**
- **架构维度抽象现在就做**（"谁决定出招"是战斗循环的必然环节，做成可插拔是设计正确）
- **schema 不建议现在加 `activation` 字段**（为不确定功能预留数据字段，违 `ADR-0004:7`）；将来加字段的成本由 ADR-0002 版本化存档迁移承担
- UI 层哑组件（`ADR-0004:12`），未来加招式按钮不改引擎

### A.9 战斗事件流字段（`ADR-0006` 需据此修订）

`ADR-0006:7` 原列"伤害、暴击、**元素标志**、伤害占比、品阶、境界"。经本会话确定：

| 字段 | 状态 | 说明 |
|---|---|---|
| `damage` 伤害数值 | 已有 | — |
| `damageRatio` 伤害占比 | 已有 | 轻/中/重/濒死 四档 |
| `isCrit` / `isDodge` | 已有 | 命中类型 |
| `element` 系别 | **修订自「元素标志」** | 七系 + 无属性（原"元素标志"是悬空字段，Round 5 找到用途：condition 修饰符需匹配它） |
| `source` 作用方式 | **新增** | 内功 / 外功 |
| `moveTier` 招式品阶 | 已有 | 下乘/中乘/上乘/绝学 |
| `attackerRealm` 境界 | 已有 | — |
| **`targetHealthTier`** | **新增（关键）** | 目标剩余生命档：满/多/半/少/危 五档，离散化 |
| **`attackerHealthTier`** | **新增（Round 11 用户追加）** | 攻击者剩余生命档，同五档 |
| **`attackerDisplayTier` / `targetDisplayTier`** | **新增（Round 12）；Round 18 改为 50 档** | 双方显示档位。**原设计七档**（不堪一击→返璞归真），**Round 18 用户决定照搬 xkx100 的 50 档**（列表见 Round 17.1）。用于**闪避文本的实力差门控**——避免"新手从容躲开大师"。⚠️ 档数变化不影响事件流结构，只影响枚举取值范围 |
| **`beastTrigger`** | **新增（Round 12）** | 兽的机制是否在本回合触发，供兽文本插入片段判断 |

**为什么这两个新增字段是"现在不定就伤筋动骨"的**：文本选词必须知道"打完还剩多少"，否则会出现"一剑穿喉后敌人继续战斗"的穿帮。`ADR-0006:12` 明写"事件结构后补会伤及存档与模拟器兼容性"。

### A.10 引擎形状清单（现在必须做，否则将来补不动）

| # | 形状要求 | 现在做成本 | 将来补成本 |
|---|---|---|---|
| 1 | 事件流携带双方剩余生命档 | 加两个离散字段 | 动存档格式 |
| 2 | `instant-damage` primitive | 加一个 primitive | 四个用法各写一遍且不一致 |
| 3 | 文本 = 带约束的多片段组合 | 抽取器支持过滤条件 | 重做战斗文本系统 |
| 4 | 出招 = intent 源可插拔 | 抽一层接口 | 战斗循环重写 |
| 5 | 战斗参与者建模为**集合**，而非"攻击者 + 受击者"两方 | 同 4 量级 | 加宠物/召唤物 = 重写战斗系统（见 Round 11）。**注：兽已确定按装备模型实现，用不上它，但抽象仍要做（≈0），保住未来升级为真单位的路径** |
| 6 | 元素/系别是 config 数组，引擎只遍历 | 已由 ADR-0004 保证 | 加第 8 系要改代码 |
| 7 | 效果 = primitive 组合，非硬编码 | 已由 ADR-0004/0005 保证 | 加效果要改引擎 |
| 8 | 伤害管线有"**应用前拦截点**"（可判断此击是否致死） | 管线形状，现在便宜 | 致命保护类效果无法表达 |
| 9 | **效果实例 = { 定义 + 运行时状态 }**，运行时状态（剩余冷却、已触发次数、剩余层数）**是存档的一部分** | 设计时就定 | 加任何带 CD 的效果都要动存档格式 + 迁移 |
| 10 | **等级/档位 = 数字 + UI 映射表**：引擎与存档只存数字，武侠词只存在于映射表（**Round 18 用户纠正**——Round 17 的"id 化"是过度设计）。映射支持**区间**（多对一）。清单见 A.17 | 设计时就定 | 若把武侠词写进引擎或存档，改词/增删档位就要动引擎与迁存档 |
| 11 | **引擎绝不按枚举的具体值分支**（Round 19 精确化）；分支条件必须**数据化**成字段 | 设计时就定 | 一旦出现 `if (element === "fire")`，加第 8 系就要改代码 |
| 12 | **加/删不对称**：加枚举成员安全（加 config 项）；删成员需**引用完整性检查**（校验器）+ 迁移（`ADR-0002`） | 设计时就定 | 删除时若不管引用，会让内容条目与存档悬空 |
| 13 | **"学习武功"流程需带可配置的条件检查点**（即使 MVP 条件恒真） | 设计时就定（Round 24 补，此前遗漏） | 将来做拜师门槛/师承限制时要改流程；复用已有 `condition` 系统，成本很低但不预留就要动流程 |

**判断原则一（用户 Round 10 明确）**：内容可以后补，**形状不能**。凡是"将来加功能需要重写引擎"的地方，现在就要把抽象做出来——即使该功能本身不做。

**判断原则二（用户 Round 17 提出、Round 18 修正）**：**名称与数量都是内容，引擎不得写死**。可以"有几层"，但改了数量不能崩。

**判断原则三（Round 24，引擎设计的硬要求）**：

> **凡是可能变化的业务规则，都要留一个可替换的边界**（策略函数／配置表／钩子）。
> 这样它就永远是"**逻辑**"问题，而不会升级成"**形状**"问题。

这条是从"形状 vs 逻辑"的区分（A.20）推出来的：
- **形状**（存档结构、抽象层次、扩展点、枚举契约）→ 改了要动架构和迁移 → **现在必须定**
- **逻辑**（具体业务规则的实现）→ 改了只改一个函数 → **可以后定**

而一个"逻辑"问题之所以会变成"形状"问题，**通常只是因为它没有被封装**。所以留边界不是可选项，是**防止问题升级的手段**。

**三条原则的关系**：
1. 原则一告诉你**什么必须现在定**（形状）
2. 原则二告诉你**什么不能写死**（名称与数量）
3. 原则三告诉你**怎么保证其余的东西永远不必现在定**（留可替换边界）

**已按此原则识别的边界**：离线致死规则（策略函数）、离线掉落后处理（`lootPostProcess` 枚举 + 参数）、波次调度（config 驱动）、目标选择（三来源优先级）、掉落链（可配置管线）、学习武功条件（A.10 第 13 条）、强度聚合函数（装备评分 / 角色战力共用）。

**具体做法（Round 18 用户方案，已推翻 Round 17 的 id 化）**：
- **等级/档位序列** → 引擎与存档存**数字**，武侠词是 **UI 映射表**。改词、增删档位都只改映射表，**引擎与存档零改动**。
- **有语义的枚举**（七系、品阶、脉、门派、primitive 类型、事件/命中类型）→ **必须有稳定 id**（被内容交叉引用）。**但引擎不得按具体值分支**——分支条件须数据化（Round 19.4），这样增删成员才不用改代码。⚠️ 稀有度不在此列，它可数值化（见 A.17）。

**唯一需要迁移的例外**：改**等级上限**（非改映射）。此时超出新上限的存档要收敛，属 `ADR-0002` 版本化迁移范畴。

**加/删不对称**：加成员安全；删成员需引用完整性检查 + 迁移（A.10 第 12 条）。

**受约束清单**：江湖序列 6 级／显示档位（**Round 18 改为 50 档**）／词缀阶位 T1–T3／伤害占比 4 档／剩余生命 5 档／秘境层数 —— 均为数字 + 映射。
**不适用**：七系／武功品阶／装备稀有度／脉／门派 —— 有语义，需 id。

### A.11 兽（驭兽，非召唤；**本质是装备，不是战斗单位**）

> **Round 13 关键修正（用户提出，助手验证通过）**：兽不参与攻击，**本质上就是一件装备**，区别是它会在战斗文本中有一席之地。此判断推翻了助手 Round 11 的 NvM 方案，成本从 ≈15% 降到 **≈5%**。

- **引擎模型 = 装备条目**：兽 = 一组修饰符 + 一段可声明的叙事片段。引擎**不需要认识"兽"**，只知道"带 narrative 的装备"。
- **战斗仍是 1v1**，不需要 NvM、不需要参与者集合（但集合抽象仍要做，见 A.10 第 5 条）。
- **通用能力推广**：兵器 → `{weaponAction}`、兽 → 兽片段、名剑 → "剑身发出一声清鸣"，**三者同类**，都是"装备在战斗叙事中的投射"。
- **边界（不能做的事）**：① 成为敌人攻击目标；② 有血量、会死；③ 在主人不出招时独立行动。石龟"替你挡下"= 减伤修饰符 + 叙事包装。
- **槽位**：独立**"随从"栏**，不占 6 个装备槽（兵/冠/甲/腕/腰/靴）。
- **形态**：驭兽（真实动物被驯养驱使）。**排除召唤物**（凭空生成，`ui-direction.md:9` 已禁符箓，`style-guide.md:22` 禁修仙语汇）。机关傀儡作第二条线留待门派设计时评估。
- **与七系关联 = 机制放大器模型**：兽不复制该系伤害，而是改造该系核心机制的某个参数（木=扩散／火=引爆／水=延长／金=维持／土=转移／风=缩短／雷=追加）。
- **MVP 范围 = 2 只**：木系毒蜂（进攻型）+ 土系石龟（防御型），一攻一守验证模型。其余五系留待验证后补（纯内容，不碰引擎）。
- **获取**：门派贡献兑换（`CONTEXT.md:106`，已有货币）。**排除任务奖励**（ADR-0008 明确不做任务体系）。
- **成长**：随境界自动成长。**不做**喂养/进化（避免第二个养成循环），留 v2。
- **兽文本**：
  - 触发 = 兽正在执行它的机制（非随机乱入）
  - 频率节流 = 概率（~25%）+ 出场冷却（~3 回合）
  - 品阶决定**片段数**（下乘 1 段 / 中乘 2 段 / 绝学 3-4 段），**不决定频率**
  - 互动四型：救援 / 配合 / 反馈（与 `{opening}` 残血词库联动，价值最高）/ 增益
  - 兽片段**插在主文本之前**（因果上先发生）
  - 架构要求很轻：事件流标记 `beastTrigger` + 组合器支持可选插入片段
- **关键能力（挡濒死）的引擎预留**：`chance` 参数 + 效果实例 CD 状态（进存档）+ 伤害管线"应用前拦截点"
- **离线结算规则**：推荐 **A. 离线战斗不致死**（离线秘境推进最多"推不动了"，不会死）。理由：致命保护是 0/1 事件，期望值失真；让死亡成为离线结果会让玩家不敢离线，与放置品类核心体验冲突。在线时照常掷骰。

### A.12 门派

- **xkx100 洞察**（`xkx100-content-structure.md:198`）：**门派 =「武功池 + 入门条件 + 辈分序列」三件套**
- **MVP 数量 = 3 个**（用户同意）。`ADR-0004:2` 引擎零写死数量（门派数 = `content/sect/` 文件数），数量是内容量问题不是引擎问题
- **区别落在战斗节奏而非颜色**：

| 门派 | 缺省系 | 节奏 | 内力特性 |
|---|---|---|---|
| 丐帮 | 火 / 雷 | 爆发型：短时间倾泻，之后疲软 | 高消耗、高回复需求 |
| 武当 | 水 / 土 | 持久型：防守反击，拖得住 | 低消耗、续航强 |
| 唐门 | 木 / 金 | 累积型：毒/破甲叠加，越打越疼 | 中等消耗、前置投入长 |

- **门派与七系 = 主系 + 副系倾向（B 方案）**：门派提供缺省系别，武功可覆盖 → 同门派内也能有不同系武功，门派身份 ≠ 系别身份，两者正交（用户原话"丐帮=火只是举例，先作为缺省"）
- **门派内「脉」（`lineageId`）= 武功池的细分标签，非玩家身份选择**：
  - 武侠正典证据：**华山剑宗/气宗**（《笑傲江湖》骨架）、**丐帮污衣堂/净衣堂**（《射雕英雄传》）
  - **反对做成 WoW 式互斥专精**（硬冲突 `ADR-0001`"流派由玩家玩出来，不预设标签"）
  - 玩家可学多个脉的武功（受贡献/前置限制），专精感由**槽位限制**（招式 4 / 心法 3）逼出来，非系统规定
  - 实现：并行字段 **`lineageId`**，与 `sectId`/`regionId` 同级，校验器照套
  - **命名已定：「脉」**（用户否决"山头"——有歧义）。排查过冲突：流/流派（= 玩家构筑，重载）、系（= 七系，重载）、宗（易与门派本身混）

- **五条引擎预留（关键，见 Round 14.4 全景表）**：
  1. 门派内进阶（贡献等级）是**运行时进度，进存档**
  2. **已学武功必须与当前门派解耦**（最关键）——已学武功是玩家的属性，门派只是获取来源；否则将来做换派/偷师时数据结构已定型
  3. `condition` 支持的匹配维度**可扩展**（含 `sectId`/`lineageId`）
  4. 门派间关系（敌对/同盟）是**内容表，不是硬编码**
  5. 门派归属是**运行时状态、可变更**（存档里是"当前门派"字段）
- **三件套的第三件 = 门派贡献等级**（推荐）：用已有货币"门派贡献"（`CONTEXT.md:106`）做门派内进阶。不需要新概念，且天然给"门派内成长"手感。**排除"境界"**（那是全局进度主轴，不是门派内的）
- **待定**：入门条件 MVP 做不做？师父（`sect.masters`，C8 缺失待补）是只留字段还是要有实际内容？

### A.13 变强反馈（补中频刻度）

**诊断**：慢反馈（境界 6 档、显示档位 7 档）有但太慢；中频（小时级）**空白**；高频（伤害数字、暴击、掉落）有。

| 补法 | 内容 | 成本 |
|---|---|---|
| **A. 强度聚合指标** | 聚合连续数值，每换装备/每提升境界都会动。与显示档位**互补**：档位给身份感（离散·叙事化），聚合值给进度感（连续·数值化）。**档位之所以显得慢，是因底下没有连续曲线托着它**。**命名：不可用「战力」（用户否决——太页游）、不可用「等级」（`CONTEXT.md:11` Avoid）；推荐「功力」**。用户若不要聚合数字，A 可整个跳过 | 中 |
| **B. 秘境推进记录** | "已推进至第 18 层（历史最佳 15 层）"，秘境分层已有设计 | 极低 |
| **C. 战斗内新纪录提示** | "最高伤害 3,847！"存几个 max 值 + 文本。**不叫"战力"，就是具体指标**（最高伤害、最长连击、单次最高中毒层数）。**保留建议：顺带记录"每一招的最高伤害"**——与 A.14 的定向强化是天然搭档：专精了某招就能看到该招纪录不断被刷新 | 极低 |
| **D. 叙事化反馈** | **战斗文本分档系统本身就是变强反馈机**：同一招弱时"血线缓缓渗出"→ 强了"皮肉焦黑"→ 更强"连人带剑被劈作两段"。**零额外成本**（分档维度已全定） | **零** |

D 条与 `style-guide.md:24` 一致：禁止"数值直出的干瘪句"，所以"造成 100 → 120 点伤害"本就不该是反馈方式，**"伤口从血线变成焦黑"才是**。

**附带**：若采用 A，`CONTEXT.md` 需补「功力」正式词条（审计发现"战力"零出现，`ui-direction.md:189` 用过一次；现改用"功力"）。

### A.14 "强化特定技能"（涌现式专精）

- **采用 C 方案：装备/词缀定向强化**，`condition: { moveId: ... }`。同时保留 **D（镶嵌/socket）的数据预留**（`ADR-0005:7` 原文已声明"MVP 不做武器 socket，数据结构预留"）。
- **不建独立专精系统**（天赋树）：成本高、声明式（冲突 `ADR-0001`）、且与词缀定向强化功能重复。
- **劝退 A 方案（熟练度）**：需跟踪每招使用次数（进存档），且**惩罚换构筑**（换了等于放弃积累），与"鼓励尝试、流派涌现"相悖。
- **它是涌现的专精，不是声明的专精**：玩家从不"选择专精"，只是反复挑选强化某招的装备；挑够三件就成了该招专精，但**系统里没有"专精"这个实体**。完全兑现 `ADR-0001`。
- **顺带提供中频反馈**：每换上一件定向词缀，玩家都能看到那一招变强。
- **引擎唯一预留：`condition` 匹配维度可扩展**，至少含：
  `element`（系别）/ `source`（内功|外功）/ `moveId`（招式）/ `moveTier`（品阶）/ `targetState`（目标状态）/ `sectId` / `lineageId`（脉）/ `equippedTag`（装备标签）/ **`equippedSet`**（套装，Round 15 新增）

### A.15 伤害计算管线（引擎核心，Round 15 定义）

> ⚠️ `ADR-0005:6` 的"固定值 → 加法百分比 → 乘法区"是**修饰符的聚合规则**，**不是**伤害的应用顺序。两者必须分开定义。

| 阶段 | 名称 | 说明 |
|---|---|---|
| 1 | `base` 基础值 | 招式/装备/境界的原始数值 |
| 2 | `+flat` 固定加成 | "火系伤害 +50" |
| 3 | `+add%` 百分比加成 | "火系伤害 +15%"（同类相加） |
| 4 | `×mult` 乘法加成 | "所有伤害 ×1.2"（异类相乘） |
| 5 | `crit` 暴击 | 暴击率判定 + 暴伤倍数 |
| 6 | `source` 攻方修正 | 内功/外功各自走对应防御 |
| 7 | `defense` 守方减免 | 护甲 / 内功防御（百分比递减） |
| 8 | `vuln/res` 易伤抗性 | 目标受伤加深、系别亲和度 |
| 9 | `dodge` 闪避判定 | 0 或 1 |
| 10 | `final` 最终值 | — |
| 11 | `post` 后续触发 | 反伤、吸血、溅射、兽的追加 |
| 12 | `apply` 应用 | 扣血 → **判定致死（拦截点）** → 触发致命保护 |

**每个 modifier 必须声明自己作用于哪个阶段。此管线必须现在定死——后改会让所有已配数值失效。**

### A.16 战斗/武学/装备穷举索引（Round 15）

完整表格见 **Round 15.3–15.7**，此处只列结论：

- **战斗形态**：建议**支持多目标**（1v1 是特例）。理由：`ADR-0005` 火系「溅射」、兽「扩散中毒」都需要多目标，若定 1v1 则这些机制是死的。`targetSelector` 需支持 单体/全体/相邻/随机 N 个/除主目标外。
- **武学效果 16 种**（见 15.4）：其中治疗、吸血需 `heal`；驱散需 `dispel`；连击需"重复执行"能力（待确认）；护盾可能需独立 primitive。
- **装备影响 12 种**（见 15.5）：套装效果需 `equippedSet` （已入 condition 可扩展清单）。
- **时间模型**：实时 tick，非回合制。离线 = tick 快进 + 稳态公式。
- **随机源 8 处**：出招权重、暴击、闪避、词缀 roll、掉落、文本选择、兽出场、致命保护。统一策略"在线掷骰、离线期望值"，**致命保护除外**（0/1 事件）。
- **存档内容 7 大类**（见 15.7 表格）：角色 / 装备 / 武学 / 状态 / 进度 / 资源 / 随机种子。

### A.17 数值化清单（Round 19 用户要求穷举）

**原则**：一切可量化的东西，引擎里存**数值**，外面保留**可后台修改的映射关系**；映射支持**区间**（多对一，如 1–5 级 → 一档）。

**需「数值 + 映射表」的 12 项**：境界等级／显示档位（50 档）／门派等级／生产活动等级／伤害占比档（轻中重濒死）／剩余生命档（满多半少危）／**稀有度（整数 0–3，其下游规则"词条数"挂在映射项内）**／词缀阶位（1–3）／武功品阶（1–4）／兽成长阶段／装备强化等级（若做）／秘境层数段（可选）

> ⚠️ **Round 20 修正**：稀有度的因果是"**先定稀有度 → 再按稀有度生成词条**"（稀有度是因，词条数是果），**不是**由词条数反推稀有度。装备生成流程见 Round 20.1。

**纯数值、不需映射**：修为／生命／内力／门派贡献／招式冷却／状态持续回合／连击数／秘境层数本体／层内进度／掉落等级／词缀数值／资源数量／战斗 tick／各项纪录／战力聚合值

**映射表结构**（Round 20 扩展）：**key 是数值，value 是一个配置对象**——`label` 只是其中一个字段，下游规则、解锁清单、呈现参数都在同一处：

```json
{ "level": 3, "label": "绝世", "affixCount": { "min": 5, "max": 6 }, "hasSpecialAffix": true }
```

- 区间形式：`[{ min, max, label, ...params }]`，支持多对一（1–5 级 → 一档）
- 表在 `content/config/`，后台可改；**引擎读对象里的字段，永远不读 `label` 做判断**
- 校验器检查：区间无缝、无重叠、覆盖全值域、**下游规则自洽**（如稀有度递增时词条数下限不得递减）

**真正标准（Round 19 精确化，取代 Round 18 的"有语义/无语义"二分）**：不是"有没有语义"，而是 **引擎是否按具体值分支**。稀有度虽"有语义"，但它的规则（词缀条数）在表里，所以可数值化；反之若引擎写 `if (element === "fire")` 就不可扩展。

**必须 id 化的三类**：primitive 类型（引擎按 id 找实现）／内容条目（`content.md:30`）／事件类型与命中类型（引擎按 id 分支，如闪避 = 0 伤害）

### A.18 秘境结构（Round 21–22）

- **层内模型 = 定时波次**（Round 21 Q44 + Round 22 追加提前规则）：
  **触发下一波 = 时间到 OR 上一波清空，取较早者**。即"清完才来"与"定时才来"的并集。
- **四参数**（全在 `content/config/`，后台可改）：`waveCount` 本层波数／`waveInterval` 波间隔／`waveComposition` 每波构成／`maxConcurrent` 场上怪物上限
- **`maxConcurrent` = 技术护栏（12–20），非难度调节**；超出则波次延后而非丢弃
- **随机化分两层**：间隔 `T ± 20%`（节奏）、构成从本层池抽（搭配）；**精英/Boss 不进随机池**。**核心原则：随机性放在节奏与搭配上，不放在难度上**
- **计时器「一炷香」不实现**（Round 21 Q45：不管设计稿）→ 无探索时长/门票系统
- **推荐境界 = 软提示**（Round 21 Q46），非硬门槛；硬门槛只用于"未通关前一层"
- **离线持续推进**（Round 21 Q47 = A）：`单波清怪时间 ≈ 波总HP / 稳态DPS`；`本层耗时 ≈ Σ清怪 + Σ间隔`；`离线可推进层数 = 离线时长 / 平均单层耗时`。推进会在卡住的层停下
- **目标选择三来源**（Round 22）：① 招式自带 `targetSelector`（内容声明）② 玩家策略配置（存档，**离线用此**）③ 实时点击（仅在线）。优先级 ③ > ② > ①
- **intent 三维度独立配置**（Round 22 关键）：`actor`／`skill`／`target` 各自可 auto 或 manual → 纯自动／**半自动（auto 出招 + manual 选目标，用户要的）**／全自动
- **层主**：每 N 层一个（如第 5、10 层），非每层都有
- **秘境解锁**：混合式——前几个链式引导，之后按推荐境界并列开放
- **待落实的 xkx100 建议**：`unlocks: <下一层 id>` 解锁链显式字段 + 适用境界区间字段（供校验器检查连通性闭环与境界段覆盖）

### A.19 装备底材与词缀池（Round 23）

- **掉落链 = 七次随机**（掉不掉 → 掉落等级 → 底材 → 稀有度 → 词缀条数 → 抽词缀 → 阶位 → 数值）+ 底材固有词缀（固定）。**必须是可配置管线，不能写死**
- **底材 = 分层方案**（⚠️ Round 24 修正，取代 23.3 的物品等级方案）：**每个底材是独立条目，声明自己的层数区间与品阶范围**，高层掉的是**另一个底材**（换名字/描述/美术），而非同一条目加 `itemLevel`。`itemLevel` 降级为可选乘数（MVP 不启用）
- **这与 `ADR-0005:2`"境界提供门槛（解锁秘境/底材）"完全吻合**——是我在 Round 23 把它过度简化成了 itemLevel，才丢了文本与美术维度
- **美术侧无需改动**：`ADR-0003:8` 的"资产按约定路径引用"天然支持每个底材一个 id + 一张图标
- **MVP 底材规模 = 36 个**（✅ Round 24 用户定：**6 槽位 × 3 层级段 × 2 变体**，即宽松版）
- **两个维度不可混**：秘境层数 → 装备有多强（itemLevel，深度）；境界 → 能进哪些秘境/哪些底材入池（gating，广度）。境界还提供裸装基础数值
- **底材倾向 = 标签化**（非权重表）：底材声明 `preferredTags`，词缀自带 `tags`，`权重 = 基础 × (1 + 匹配标签数 × 加成)`。新增词缀零改动，兑现 `ADR-0008:15`
- **特色词缀 = 机制改变型**（非数值），复用兽的"机制放大器"模型（`condition + primitive`），引擎零新增
- **洗练上限**：MVP 数值只由 tier 决定（用户判断：低层掉不出 T3，天花板天然受限）；保留 `itemLevel` 系数作乘数配置（默认 1.0）以备将来开启后期数值成长
- **离线掉落 = 玩家可选**：`lootPostProcess: keepBest | allMaterials | lootBox | keepAll`。共用"生成 → 后处理"管线
- **词缀池形态**：20+ 个词缀条目，每个内含 3 阶数值表（**有意偏离** `content.md:30` 的"每条目一文件"）
- **掉落提示**：击杀时浮动提示 + 探索结算时汇总展示（按品阶/稀有度排序）；掉落记录仅在会话内存活，不进存档
- **MVP 底材规模 = 36 个**（6 槽位 × 3 层级段 × 2 变体），共约 252 项内容

### A.20 形状 vs 逻辑（Round 24 方法论——评估待定项时先问这个）

| | **形状（shape）** | **逻辑（logic）** |
|---|---|---|
| 是什么 | 存档结构、抽象层次、扩展点、枚举契约 | 具体业务规则的实现代码 |
| 改了的后果 | **动架构 + 数据迁移** | 只改一个函数／模块 |
| 例子 | 事件流字段、primitive 契约、效果实例运行时状态、有序集合 id 化 | 离线致死用哪条规则、掉落权重怎么算、波次调度怎么排 |

**用法**：评估任何待定项时，**先问"它动的是形状还是逻辑"**。
- 形状 → **现在必须定**，后补要迁移
- 逻辑 → **可以后定**，只要它被封装成可替换的策略边界

**推论**（⚠️ 已提升为 **A.10 判断原则三**，是本项目的**引擎设计硬要求**）：**凡是可能变化的业务规则，都要留一个可替换的边界**（策略函数／配置表／钩子），这样它永远是"逻辑"问题，不会升级成"形状"问题。

三条判断原则合起来构成一个决策体系，详见 A.10：
1. **原则一**——什么必须现在定（形状）
2. **原则二**——什么不能写死（名称与数量）
3. **原则三**——怎么保证其余的永远不必现在定（留可替换边界）

**由此补出的 A.10 第 13 条**：「学习武功」流程需带可配置的条件检查点（即使 MVP 条件恒真）——这是用户质疑"你确定不影响引擎吗"之后发现的遗漏。

**五个待定小项的精确评估与默认取值**：见 Round 24.5。
- 离线致死 → A 离线不死（策略函数封装）
- 门派入门条件 → MVP 恒真（流程留检查点）
- 师父 → 先只留字段
- 江湖序列 → 新秀/好手/高手/名家/宗师/泰斗
- 战力命名 → 继续搁置（函数要有）

---

