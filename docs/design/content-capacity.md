# 内容承载力分析：IdleRPG 规划 vs xkx100 内容资产

> **状态**：本轮决策记录，**尚未整合**进 `CONTEXT.md` 与 ADR。待后续决定是否并入（可能的落地形式：ADR 修订 + 术语表补充 + `docs/agents/content.md` 字段说明）。
> 依据：`docs/research/xkx100-kungfu-combat.md`、`docs/research/xkx100-content-structure.md`。
> 决策日期：2026-08-30。

## 一、结论速览

| 判断 | 内容 |
|---|---|
| **可直接承载** | 战斗文本矩阵（伤害类型×档位、闪避/招架模板）、招式四字格与意象系统、内容组织与校验思路 |
| **有缺口、已定承载方案** | 武功前置与伤害类型、NPC 概念、品阶显示、内容归属字段、生产技能等级 |
| **明确不承载（有意不做）** | 任务体系、知识/生活技能、NPC 独立集合 |
| **需新增 backlog 票** | 图鉴自动生成 |

**总体判断**：按现规划完成后，可以承载 xkx100 级别内容中的**武功体系、装备/物品、战斗文本、区域与怪物、门派与师承**五大块；不承载的是**任务与知识技能**（MVP 本就不做，属有意取舍而非能力缺陷）。

## 二、缺口矩阵

| xkx100 资产 | 我们现有规划 | 缺口 | 承载决策 |
|---|---|---|---|
| 武功：招式表 + 学习前置 + 四维效果区间 + 伤害类型 | `content/martial/`（kind / 品阶 / 标签 / 冷却） | 无前置、无伤害类型、无效果区间 | 加 `prerequisites` 与 `damageType` 两字段；效果数值**生成期静态化**（品阶×招式序号经 config 公式算好写入），运行时修饰符只读 |
| NPC 四类（师父/商人/任务发布人/氛围） | 只有 `monster/` | 无 NPC 概念 | 不立集合。师父 = `sect/` 条目的 `masters` 字段（承载可教武功池与贡献规则）；氛围怪靠 monster `tags` 表达；商人与任务发布人 MVG 不做 |
| 任务四元组 + 24 种阶梯 + questobj | 无 | MVP 不做 | 不预留 schema（任务形态未定，预留大概率返工）；吸收模式：**纯叙事道具 = 普通条目 + `tags:["quest"]` + 价值归零**（标签代替特殊类型） |
| 知识/生活技能 30 余种 | `activities` config（无等级） | 无技能等级 | activities 条目加 `level` / `xp` 字段（顺带满足 #6 采药区按等级解锁）；读书医术类明确不做 |
| 品阶显示 50 档 | 无显示档位概念 | 数值→中文描述的推导 | config 加 `displayTiers`（数值区间 → 中文造诣名），战斗文本与 UI 实时推导，与掉落品级**正交** |
| 内容三层放置（通用/门派/区域） | `content/` 平铺集合 | 无归属维度 | 保持平铺 + 条目 `sectId` / `regionId` 字段 + 校验器检查覆盖与连通（不做子目录分层，便于编辑器与 agent 批量操作） |
| 图鉴自动生成（一集合一分册） | 无 | 功能缺口 | 新增 backlog 票（依赖 #15 编辑器或独立小票），schema 无需改动 |
| 战斗文本矩阵（5 类伤害 × 6~9 档 + 闪避/招架/格挡模板） | `content/combat-text/` 规划中 | 无缺口 | 直接落位：模板为 JSON 条目 + 槽位变量，词库按档位分池，与 xkx100 `$N/$n/$w/$l` 槽位协议同构 |
| 效果系统表达力（四维插值） | 修饰符聚合引擎 | 待确认 | 生成期静态值 + config 公式即可表达，运行时插值留 v2（无需 prototype 验证） |

## 三、决策清单

1. **武功 schema 轻量扩展**：加 `prerequisites`（先修武功 + 等级）与 `damageType`（割/刺/瘀/内伤/抓伤），效果数值生成期静态化
2. **不立 NPC 集合**：师父归 `sect.masters`，氛围定位走 monster `tags`
3. **不预留任务 schema**：吸收“标签代替特殊类型”原则进 `docs/agents/content.md`
4. **activities 加等级**：`level` / `xp`，服务采药区解锁；知识类技能不做
5. **显示档位与掉落品级正交**：config `displayTiers` 推导，不动掉落体系
6. **内容平铺 + 归属字段**：`sectId` / `regionId`，校验器做覆盖与连通检查
7. **图鉴自动生成进 backlog**：新增票，不阻塞现有票
8. **武功品阶武侠化**：`下乘 / 中乘 / 上乘 / 绝学`（替换原 黄/玄/地/天 的网文仙侠语汇）
9. **装备稀有度武侠化**：`寻常 / 精良 / 罕见 / 绝世`（替换 普通/魔法/稀有/传说），词缀条数规则不变（0 / 1~2 / 3~4 / 5~6）

## 四、命名语汇表

| 维度 | 档位 | 说明 |
|---|---|---|
| 武功品阶（功法档次） | 下乘 → 中乘 → 上乘 → 绝学 | 金庸体系“上乘武学”的说法；可平滑扩至第五档 |
| 装备稀有度（器物档次） | 寻常 → 精良 → 罕见 → 绝世 | 仅决定词缀条数；颜色映射沿用灰/青/紫/金，去掉“传说” |
| 显示档位（数值→造诣描述） | 不堪一击 → 初窥门径 → 稍有所成 → 登堂入室 → 炉火纯青 → 出神入化 → 返璞归真 | 从数值实时推导，供战斗文本与 UI 使用；剔除仙侠语汇 |

三套语汇互不冲突：武功看“功法档次”，装备看“器物档次”，档位看“造诣深浅”。

## 五、新增 backlog 候选票

- **图鉴**：由内容集合自动生成（一集合一分册，索引即图鉴）；schema 无改动，依赖 #15 编辑器或作为独立小票。

## 六、待整合项（本轮不动，后续决定）

- `CONTEXT.md`：是否新增「品阶 / 稀有度 / 显示档位 / 前置 / 归属」等术语条目
- `docs/agents/content.md`：字段说明（prerequisites / damageType / sectId / regionId / tags 原则）
- ADR：若上述取舍被认为“改动成本高、事后会疑惑”，再升格为 ADR（例如 ADR-0008：内容承载边界）
- 命名空间变更的连锁：现有内容文件尚无品阶/稀有度实例，改名**不破坏任何已发布 id**，可安全执行
